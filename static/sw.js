const CACHE='idspl-static-v17';
const BASE=new URL('./',self.registration.scope).pathname.replace(/\/$/,'');
const asset=path=>BASE+path;
const STATIC=[asset('/offline'),asset('/manifest.webmanifest'),asset('/static/app-shell.css'),asset('/static/app-shell.js'),asset('/static/Initiative%20logo.png'),asset('/static/icons/icon-192.png'),asset('/static/icons/icon-512.png'),asset('/static/icons/icon-maskable-512.png')];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(STATIC)));self.skipWaiting()});
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET')return;const requestUrl=new URL(request.url);if(requestUrl.origin!==self.location.origin)return;const relative=requestUrl.pathname.startsWith(BASE)?requestUrl.pathname.slice(BASE.length):requestUrl.pathname;if(request.mode==='navigate'){event.respondWith(fetch(request,{cache:'no-store'}).catch(()=>caches.match(asset('/offline'))));return}if(!(relative.startsWith('/static/')||relative==='/manifest.webmanifest'))return;event.respondWith(caches.open(CACHE).then(async cache=>{const cached=await cache.match(request);const network=fetch(request).then(response=>{if(response.ok&&response.type==='basic')cache.put(request,response.clone());return response}).catch(()=>cached);return cached||network}))});
