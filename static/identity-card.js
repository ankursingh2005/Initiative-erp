(() => {
  'use strict';
  const $ = id => document.getElementById(id);
  const base = location.pathname.startsWith('/erp/') ? '/erp' : '';
  const token = localStorage.getItem('token');
  if (!token) { location.replace(base + '/login'); return; }
  let cards = [], canEdit = false, selected = null, draft = null, dirty = false, busy = false, photoVersion = 0;
  const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const initials = name => name.split(/\s+/).filter(Boolean).slice(0,2).map(s=>s[0]).join('').toUpperCase();
  const validPhoto = photo => typeof photo === 'string' && /^data:image\/(jpeg|png|webp);base64,[A-Za-z0-9+/=]+$/.test(photo);

  function message(text, error=false) { $('message').textContent=text; $('message').classList.toggle('error', error); }
  function cardHtml(card) {
    const photo = validPhoto(card.photo) ? `<img src="${card.photo}" alt="${esc(card.employee_name)} portrait">` : '<span class="photo-placeholder"><svg viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="2"><circle cx="24" cy="15" r="8"/><path d="M8 44v-5a16 16 0 0 1 32 0v5"/></svg>Add photo</span>';
    return `<div class="card-side"><div class="side-label"><span>01 / FRONT</span><span>EMPLOYEE IDENTITY</span></div><article class="id-card id-front" aria-label="Front of identity card"><div class="card-top"><div class="brand-lockup"><strong>Initiative</strong><small>EMPLOYEE IDENTITY CARD</small></div><div class="logo-badge"><img src="${base}/static/Initiative%20logo.png" alt="Initiative Electronics Megastore logo"></div></div><div class="gold-rule"></div><div class="portrait">${photo}</div><div class="card-tag">One team. One Initiative.</div><dl class="card-data"><dt>Emp ID :-</dt><dd>${esc(card.employee_id)}</dd><dt>Emp Name</dt><dd class="name">${esc(card.employee_name)}</dd><dt>Designation</dt><dd>${esc(card.designation)}</dd><dt>Mob No.</dt><dd>${esc(card.mobile || 'Not added')}</dd></dl><footer>INITIATIVE DATA SYSTEM PRIVATE LIMITED</footer></article></div><div class="card-side"><div class="side-label"><span>02 / BACK</span><span>COMPANY DETAILS</span></div><article class="id-card id-back" aria-label="Back of identity card"><div class="back-art"></div><div class="back-brand"><strong>Initiative</strong><span>CONNECTED BY PURPOSE</span></div><div class="back-company"><span class="tiny">OUR IDENTITY</span><h3>Initiative Data System Private Limited</h3><h4>Head Office</h4><p>Ashok Marg, Hazratganj<br>Lucknow - 226001</p><p class="back-email">initiative.lucknow@gmail.com</p></div><div class="back-id"><span>EMPLOYEE IDENTIFICATION</span>${esc(card.employee_id)}</div></article></div>`;
  }
  function fitCards(root) {
    root.querySelectorAll('.id-front').forEach(card => {
      const data=card.querySelector('.card-data'), footer=card.querySelector('footer');
      let size=parseFloat(getComputedStyle(data).fontSize);
      while (data.getBoundingClientRect().bottom > footer.getBoundingClientRect().top-12 && size>7) {
        size-=.5; data.style.fontSize=size+'px'; data.style.rowGap=Math.max(4,size-2)+'px';
        data.querySelector('.name').style.fontSize=(size+1)+'px';
      }
    });
  }
  function renderPreview() { if(!draft)return; $('preview').innerHTML=cardHtml(draft); fitCards($('preview')); }
  function visibleCards(){const query=$('search').value.trim().toLowerCase(),status=$('statusFilter').value;return cards.filter(c=>(!status||c.status===status)&&[c.employee_name,c.employee_id,c.designation,c.outlet].join(' ').toLowerCase().includes(query));}
  function renderPeople(){const visible=visibleCards();$('count').textContent=visible.length;$('printAll').disabled=!visible.length||busy;$('people').innerHTML=visible.map(c=>`<button type="button" class="person ${selected===c.user_id?'selected':''}" data-user="${c.user_id}" aria-pressed="${selected===c.user_id}"><span class="person-avatar">${validPhoto(c.photo)?`<img src="${c.photo}" alt="">`:esc(initials(c.employee_name))}</span><span class="person-copy"><b>${esc(c.employee_name)}</b><small>${esc(c.employee_id)} · ${esc(c.status)}</small><small>${esc(c.designation)}</small></span></button>`).join('')||'<p class="empty">No matching users.</p>';}
  function selectCard(id){if(busy)return;if(dirty&&!confirm('Discard unsaved card changes?'))return;const card=cards.find(c=>c.user_id===id);if(!card)return;photoVersion++;selected=id;draft={...card};dirty=false;$('unsaved').hidden=true;$('selectedName').textContent=card.employee_name;$('recordStatus').textContent=card.status==='Active'?(card.saved?'Saved card':'Ready to personalise'):'Inactive account';$('editor').hidden=!canEdit;$('readOnly').hidden=canEdit;$('printOne').disabled=false;$('photo').value='';for(const key of ['employee_id','employee_name','designation','mobile'])$('editor').elements[key].value=card[key];message('');renderPeople();renderPreview();}
  function setDirty(){dirty=true;$('unsaved').hidden=false;message('Preview updated. Save your changes before printing.');renderPreview();}
  $('people').addEventListener('click',e=>{const button=e.target.closest('[data-user]');if(button)selectCard(Number(button.dataset.user));});
  $('search').addEventListener('input',renderPeople);$('statusFilter').addEventListener('change',renderPeople);
  $('editor').addEventListener('input',e=>{if(e.target.name&&draft){draft[e.target.name]=e.target.value;setDirty();}});
  $('reset').onclick=()=>{if(dirty&&!confirm('Discard unsaved card changes?'))return;dirty=false;selectCard(selected);};
  $('removePhoto').onclick=()=>{photoVersion++;draft.photo=null;$('photo').value='';setDirty();};
  $('photo').onchange=async()=>{const file=$('photo').files[0];if(!file)return;const version=++photoVersion,userId=selected;if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>3_000_000){message('Choose a JPG, PNG, or WebP photo up to 3 MB.',true);$('photo').value='';return;}try{const data=await new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(file);});const img=await new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=reject;image.src=data;});if(img.width*img.height>16_000_000)throw Error('Photo must be 16 megapixels or smaller.');if(version!==photoVersion||userId!==selected)return;const canvas=document.createElement('canvas');canvas.width=480;canvas.height=480;const ctx=canvas.getContext('2d');ctx.fillStyle='#fff';ctx.fillRect(0,0,480,480);const side=Math.min(img.width,img.height);ctx.drawImage(img,(img.width-side)/2,(img.height-side)/2,side,side,0,0,480,480);draft.photo=canvas.toDataURL('image/jpeg',.88);setDirty();}catch(error){message(error.message||'Unable to read this photo. Try another image.',true);}};
  async function request(path,options={}) {const response=await fetch(base+'/api/identity-cards'+path,{...options,headers:{Authorization:'Bearer '+token,'Content-Type':'application/json',...options.headers},cache:'no-store'});const data=await response.json().catch(()=>({}));if(!response.ok){const detail=Array.isArray(data.detail)?data.detail.map(d=>d.msg).join('; '):data.detail;throw Error(detail||'Unable to complete this request.');}return data;}
  $('editor').onsubmit=async e=>{e.preventDefault();if(busy||!draft)return;busy=true;photoVersion++;$('editor').querySelectorAll('button,input').forEach(el=>el.disabled=true);$('printOne').disabled=true;renderPeople();$('save').textContent='Saving…';message('Saving card details…');try{const payload={};for(const key of ['employee_id','employee_name','designation','mobile','photo'])payload[key]=draft[key];const saved=await request('/'+selected,{method:'PUT',body:JSON.stringify(payload)});cards=cards.map(c=>c.user_id===saved.user_id?saved:c);dirty=false;busy=false;selectCard(saved.user_id);message('Card details saved successfully.');}catch(error){message(error.message,true);}finally{busy=false;$('editor').querySelectorAll('button,input').forEach(el=>el.disabled=false);$('printOne').disabled=false;$('save').textContent='Save card details';renderPeople();}};
  async function printCards(list){if(dirty){message('Save or discard your changes before printing.',true);return;}if(!list.length)return;const area=$('printArea');area.innerHTML=list.map(c=>'<section class="print-pair">'+cardHtml(c)+'</section>').join('');area.setAttribute('aria-hidden','false');area.style.cssText='display:block;position:fixed;left:-10000px;top:0';await Promise.all([...area.querySelectorAll('img')].map(img=>img.decode().catch(()=>{})));fitCards(area);area.removeAttribute('style');window.print();}
  $('printOne').onclick=()=>printCards(cards.filter(c=>c.user_id===selected));$('printAll').onclick=()=>printCards(visibleCards());
  addEventListener('beforeprint',()=>fitCards($('printArea')));
  addEventListener('afterprint',()=>{$('printArea').innerHTML='';$('printArea').setAttribute('aria-hidden','true');});
  addEventListener('beforeunload',e=>{if(dirty){e.preventDefault();e.returnValue='';}});
  request('').then(data=>{cards=data.cards;canEdit=data.can_edit;renderPeople();if(cards.length)selectCard(cards[0].user_id);else{$('recordStatus').textContent='No cards';$('preview').innerHTML='<p class="empty">No eligible users were found.</p>';}}).catch(error=>{$('people').innerHTML='<p class="empty">Unable to load users. Refresh to retry.</p>';$('recordStatus').textContent='Unavailable';message(error.message,true);});
})();
