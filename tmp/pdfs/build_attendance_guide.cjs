const fs=require('fs');
const old=fs.readFileSync('tmp/pdfs/build_guide.cjs','utf8');
const helpers=old.slice(old.indexOf('const esc ='),old.indexOf('const profit='));
const {parse,flow}=new Function(helpers+';return {parse,flow};')();
let css=fs.readFileSync('tmp/pdfs/ageing_stock_guide.html','utf8').match(/<style>([\s\S]*?)<\/style>/)[1];
css+='.page{height:297mm;min-height:297mm;font-size:11pt;line-height:1.42}h2{font-size:23pt}.diagram{max-height:125px}.cover .diagram{max-height:none}.cover h1{font-size:44pt}.compact{font-size:10pt!important}.compact td{padding:8px}.page .foot{font-size:8pt}.cover .hero{margin:32px 0}';
const sections=fs.readFileSync('ATTENDANCE_USER_GUIDE.md','utf8').split(/^## /m).slice(1);
const flows=[['Sign in','Attendance','Check account'],['GPS on','Allow camera','Check outlet'],['Punch In','Fresh selfie','Confirm saved'],['Punch Out','Fresh selfie','Check times'],['Choose status','Save','Check result'],['Choose month','Previous days','View / download'],['Choose date','Set filters','View employees'],['Open employee','Choose date','Check details'],['Check filters','Download','Open file'],['Read message','Fix the cause','Verify record']];
const hero=`<svg class="diagram" viewBox="0 0 700 200" xmlns="http://www.w3.org/2000/svg"><g font-family="Arial" text-anchor="middle">${[['PUNCH IN','Start your day','#176caa','09:30'],['SELFIE + GPS','Verify each punch','#008678','YOU'],['PUNCH OUT','Complete your day','#7150ad','18:00']].map((a,i)=>`<rect x="${10+i*232}" y="5" width="216" height="185" rx="14" fill="#f0f6f8"/><circle cx="${118+i*232}" cy="70" r="43" fill="${a[2]}"/><text x="${118+i*232}" y="79" font-size="23" font-weight="bold" fill="white">${a[3]}</text><text x="${118+i*232}" y="142" font-size="16" font-weight="bold" fill="${a[2]}">${a[0]}</text><text x="${118+i*232}" y="169" font-size="14" fill="#17354b">${a[1]}</text>`).join('')}</g></svg>`;
let html=`<!doctype html><html><head><meta charset="utf-8"><title>Attendance - Illustrated User Guide</title><style>${css}</style></head><body><section class="page cover"><div class="top">INITIATIVE ERP / IDSPL</div><span class="pill">A SIMPLE GUIDE FOR EVERYDAY USERS</span><h1>Attendance</h1><p class="lead">Mark your day. Check your records.<br>Keep every punch clear.</p><div class="hero">${hero}${flow(['Open Attendance','Punch in','Punch out','Review history'])}<p class="caption">Illustrated workflow. Times shown are examples only.</p></div><h3>Follow one small step at a time</h3><ul><li>Location, camera, punch in, and punch out</li><li>Leave, weekly off, history, and monthly downloads</li><li>Admin dashboard, employee details, and reports</li></ul><p class="small">Illustrations are teaching examples, not live employee records.<br>Based on project source reviewed on 17 September 2026.</p><div class="foot"><span>BEGINNER USER GUIDE</span><span>01 / 11</span></div></section>`;
sections.forEach((s,i)=>html+=`<section class="page ${i===9?'compact':''}"><div class="top">ATTENDANCE / USER GUIDE</div>${flow(flows[i])}${parse(s)}<div class="foot"><span>INITIATIVE ERP | Step-by-step instructions</span><span>${String(i+2).padStart(2,'0')} / 11</span></div></section>`);
fs.writeFileSync('tmp/pdfs/attendance_guide.html',html+'</body></html>');
fs.writeFileSync('tmp/pdfs/render_attendance_pdf.ps1',fs.readFileSync('tmp/pdfs/render_pdf.ps1','utf8').replaceAll('Profitability_User_Guide.pdf','Attendance_User_Guide.pdf').replace('"page-$($i+1).png"','"attendance-page-$($i+1).png"'));
console.log('Built 11-page attendance guide layout.');
